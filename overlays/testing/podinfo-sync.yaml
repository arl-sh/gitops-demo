apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: podinfo-sync
  namespace: apps-sync
spec:
  dependsOn:
  - name: config-maps
  interval: 30s
  path: ./apps/overlays/testing/podinfo
  prune: true
  sourceRef:
    kind: GitRepository
    name: app-manifests
  targetNamespace: apps
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: podinfo-version
